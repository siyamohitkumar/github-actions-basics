name: Basics

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest


    steps:
      - name: Print Message
        run: echo "Hello Github Actions"

      - name: Print Greet
        run: echo "Good Morning DNK Skills"